<%= form_with(model: likedsubmission) do |form| %>
  <% if likedsubmission.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(likedsubmission.errors.count, "error") %> prohibited this likedsubmission from being saved:</h2>

      <ul>
        <% likedsubmission.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
